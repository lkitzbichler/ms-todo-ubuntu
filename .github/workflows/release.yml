name: Build & Release (Linux)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write  # erlaubt Upload an Releases

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      # Optional: nur nötig, wenn du AppImage zusätzlich baust
      - name: Install system deps (AppImage runtime)
        run: |
          sudo apt-get update
          sudo apt-get install -y libfuse2 || true

      - name: Install dependencies
        run: npm ci

      # WICHTIG: electron-builder darf NICHT publishen → --publish never
      - name: Build artifacts (.deb + AppImage)
        run: npx electron-builder --linux deb AppImage --publish never
        # Falls du KEIN AppImage willst, nimm:
        # run: npx electron-builder --linux deb --publish never

      - name: Generate checksums
        run: |
          cd dist
          sha256sum *.deb  > SHA256SUMS.txt || true
          sha256sum *.AppImage >> SHA256SUMS.txt || true
          ls -lh

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.deb
            dist/*.AppImage
            dist/SHA256SUMS.txt
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
