## Install (Ubuntu/Debian)
Download the latest `.deb` from the [Releases](../../releases) page and run:
```bash
sudo apt install -y ./ms-todo_1.x.x_amd64.deb



Warning: If you want to use an app image install `libfuse2`

# 7) Optional: Manual Release (without Actions)
Build local:
```bash
npm run build
```
